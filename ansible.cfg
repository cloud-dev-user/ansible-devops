[defaults]

inventory = inventory/hosts
deprecation_warnings = False

[colors]

warn = yellow


[inventory]
# enable inventory plugins, default: 'host_list', 'script', 'auto', 'yaml', 'ini', 'toml'
enable_plugins = host_list, virtualbox, yaml, constructed, ini, toml, aws_ec2
